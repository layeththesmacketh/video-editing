{
  "name": "lattimore-video-editing",
  "version": "0.1.0",
  "private": true,
  "description": "Claude-driven video editing service for Nick Lattimore — aggregator + taste layer over ButterCut.",
  "license": "MIT",
  "type": "module",
  "main": "server/index.js",
  "bin": {
    "lattimore-mcp": "server/index.js"
  },
  "scripts": {
    "start": "node server/index.js",
    "test": "node --test tests/*.mjs || true"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.8"
  }
}
